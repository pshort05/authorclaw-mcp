name: Release

on:
  push:
    tags:
      - 'v[0-9]*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --no-fund

      - name: Production audit gate
        run: npm audit --omit=dev --audit-level=high

      - name: Test
        run: npm run test:run

      - name: Build
        run: npm run build

      - name: Create archive
        run: tar -czf authorclaw-mcp-${{ github.ref_name }}.tar.gz dist package.json README.md LICENSE CHANGELOG.md

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/pshort05/authorclaw-mcp:latest
            ghcr.io/pshort05/authorclaw-mcp:${{ steps.version.outputs.VERSION }}
          build-args: |
            VERSION=${{ steps.version.outputs.VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: authorclaw-mcp-${{ github.ref_name }}.tar.gz
          generate_release_notes: true
          body: |
            ## Docker image

            ```bash
            docker pull ghcr.io/pshort05/authorclaw-mcp:${{ steps.version.outputs.VERSION }}
            ```

            See the [installation docs](https://github.com/pshort05/authorclaw-mcp/blob/main/docs/installation.md) for full setup instructions.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
